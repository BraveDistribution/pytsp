def had_tsp(graph):
    """
    This method is used in real warehouses, just a simple method where it orders the points lexicographically and then
    visit thems by the shortest route.
    Returns:
        Path
    """
    for node in range(len(graph-1), 0, -1):
        pass



